<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:view="clr-namespace:SwipeCardView.Sample.View;assembly=SwipeCardView.Sample"
             x:Class="SwipeCardView.Sample.View.LoginPage"
             Title="Bootleg Tinder">

    

    <!--Shell.NavBarIsVisible="False">-->
    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
            <!--<Button VerticalOptions="Center" Text="Create accaunt" Command="{Binding createCommand}"/>
            <Button VerticalOptions="Center" Text="sign in" Command="{Binding LoginCommand}"/>-->
            <Button Text="Create Accaunt" Command="{Binding NavigateCommand}" CommandParameter="{x:Type view:NewItemPage}"></Button>
            <Button Text="Sign in" Command="{Binding NavigateCommand}" CommandParameter="{x:Type view:AboutPage}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>